fun max(a, b) {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

let num1 = 42;
let num2 = 35;

print("Calculando el máximo entre dos números:");
let result = max(num1, num2);
print("El máximo es: " + result);
