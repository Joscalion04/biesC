let {
    const isPositive = (n) => 
    let {
        const result = if (n > 0) then "Positivo" else "Negativo"
    } in {
        print(result)
    }
} in {
    let greet = input("Ingrese numero: ")
    print(isPositive(number))
}