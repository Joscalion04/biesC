let x = 10;
const y = 20;

fun add(a, b) {
    return a + b;
}

fun multiply(a, b) {
    return a * b;
}

if (x > 5) {
    let result = add(x, y);
} else {
    let result = multiply(x, y);
}

add(5, 10);
multiply(x + 1, y * 2);